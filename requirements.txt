pydantic==2.5.0
fastapi==0.104.1
uvicorn==0.23.2
mangum==0.17.0
pytest==8.4.1
pytest-asyncio==1.3.0
tzdata==2024.1
skyfield>=1.53  # Used for on-demand ephemeris generation when swisseph is not available
# swisseph==2.10.3.2  # Optional - preferred for on-demand ephemeris generation (requires compilation)
